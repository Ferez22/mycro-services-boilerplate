services:
  backend:
    ports:
      - "5005:5005"
    environment:
      - ENV=production
      - API_URL=http://backend:5005

  frontend:
    ports:
      - "80:3000"
    environment:
      - ENV=production
      - API_URL=http://backend:5005
