services:
  backend:
    ports:
      - "8000:8000"
    environment:
      - ENV=production
      - API_URL=http://backend:8000

  frontend:
    ports:
      - "80:3000"
    environment:
      - ENV=production
      - API_URL=http://backend:8000
