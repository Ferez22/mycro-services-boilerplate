services:
  backend:
    ports:
      - "5000:5000"
    environment:
      - ENV=production
      - API_URL=http://backend:5000

  frontend:
    ports:
      - "80:3000"
    environment:
      - ENV=production
      - API_URL=http://backend:5000
