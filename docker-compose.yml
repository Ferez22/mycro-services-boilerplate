version: "3.9"

services:
  backend:
    build: ./backend
    networks:
      - ze-net

  frontend:
    build: ./frontend
    networks:
      - ze-net

networks:
  ze-net:
    driver: bridge
    external: true
