# Environment Variables Template
# Copy this to .env and update with your actual values

# ============================================
# Cloudflare Tunnel Configuration
# ============================================
# Get your token from: https://one.dash.cloudflare.com/
# Navigate to: Access → Tunnels → Your Tunnel → Configure
CF_TUNNEL_TOKEN=your_cloudflare_tunnel_token_here

# ============================================
# Database Configuration
# ============================================
# These are defaults for development
# CHANGE THESE IN PRODUCTION!
DB_USER=postgresadmin
DB_PASSWORD=change_this_in_production_use_strong_password
DB_NAME=postgres
DB_HOST=postgres-db
DB_PORT=5432

# ============================================
# Backend API Configuration
# ============================================
# For Docker internal communication (server-side rewrites)
# This URL is used by the Next.js server to proxy requests
API_URL=http://backend:5005

# ============================================
# Usage Instructions
# ============================================
# 1. Copy this file: cp env.example .env
# 2. Edit .env with your actual values
# 3. Never commit .env to git (already in .gitignore)
# 4. Deploy: docker compose -f docker-compose.yml -f docker-compose.prod.yml up -d

