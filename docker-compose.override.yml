services:
  backend:
    ports:
      - "5000:5000" # expose for debugging
    volumes:
      - ./backend:/app # live code mounting

  frontend:
    ports:
      - "3000:3000"
    volumes:
      - ./frontend:/app
    environment:
      - API_URL=http://backend:5000
